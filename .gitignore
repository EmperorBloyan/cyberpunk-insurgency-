# --- System / OS Files ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Rust / Backend (Cargo & Bolt) ---
/target/
/.anchor/
/.bolt/
**/*.rs.bk
test-ledger/
.yalc/

# --- JavaScript / Frontend (Node & Build) ---
node_modules/
/build/
/dist/
/.next/
.yarn/
.pnpm-debug.log
yarn-error.log

# --- Environment & Secrets (CRITICAL) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# --- BOLT Specific Artifacts ---
# We ignore the large binary artifacts but keep the generated IDLs 
# if you want them version-controlled. Usually, it's safer to ignore 'target' entirely.
